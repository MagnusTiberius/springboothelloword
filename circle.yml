machine:
  python:
    version: 2.7.3
  services:
    - docker

dependencies:
  pre:
    - sudo pip install --upgrade pip
    - sudo pip install awscli
  override:
    - docker info
    - docker build --rm=false -t $AWS_ACCOUNT_ID.dkr.ecr.us-east-1.amazonaws.com/helloworld:$CIRCLE_SHA1 .
